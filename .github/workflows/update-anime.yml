name: 更新动画数据

on:
  schedule:
    # UTC+8 每天凌晨 2:00
    - cron: '0 18 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Update anime data
        run: node scripts/update-anime.mjs

      - name: Force-add generated data files
        run: |
          git add -f src/data/bangumi-data.json src/data/bilibili-data.json || true

      - name: Commit updated anime data
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'chore: update anime data'
          commit_user_name: 'github-actions[bot]'
          commit_user_email: 'github-actions[bot]@users.noreply.github.com'
          skip_dirty_check: true
